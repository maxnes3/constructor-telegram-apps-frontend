export type PrototypeType = {
  id: string;
  modelName: string;
  aspectRatioX: number;
  aspectRatioY: number;
  borderWidth: string;
  borderRadius: string;
};

export const PROTOTYPE_ID = 'prototype';
